# simple_nodejs_login_passport

A basic nodejs + express application for user registration/login. This uses 'passport' library to manage login state in browser session.

Hey git, I've had this one changed!